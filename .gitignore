# Claude Code local config and architecture notes — keep private
.claude/

# Environment — contains real API keys, never commit this
.env

# Database file — this is your local data, not source code
*.db
*.db-shm
*.db-wal

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# macOS
.DS_Store

# Local archives and scratch files — not part of the codebase
describe_bills_archive_v1.md

#Candidate-related scraper
candidate_scraper_README.md

# Temporary working files for taxonomy seeding HITL — local only, ~16MB total
# Delete this folder once all 22 categories are approved in policy_taxonomy.json
taxonomy_working/
